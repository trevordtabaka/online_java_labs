Answer the following questions:

1.) Using the command line, how do you:
    a. change directories? cd <path>
    b. list the contents of a folder? ls, ls -al, etc inside folder directory
    c. print the path of the current directory? pwd
    d. move a file from one location to the another? mv <file or path and file> <file destination path>
    e. copy a file from one location to another? cp <file or path and file> <file destination path>
    f. create a file? touch <filename>
    g. create a folder? mkdir <foldername>
    h. delete a file? rm -rf <filename>
    i. delete a folder? rm -rf <foldername>
    j. print the contents of a file? cat <filename>
    k. edit the contents of a file? many ways, ex. vim <filename>, i to insert,
     :wq to write save and quit back out

2.) Why do we use git and Github?
Good way to have multiple people working on the same code base to keep from having
conflicts and always have stable code. You can also share your projects with others anywhere.

3.) What is the general flow of git?


4.) What is the purpose of adding a remote to a local git repository?

5.) How do you print a message to the console?
 System.out.println("messagehere");

6.) Where does a program begin its execution?
 main method unless otherwise specified

7.) What is a code block and how do you create one?
Its code that is all contained to the block as far as what it i. Use {} to make a block.

8.) What is the idea of variable scope?
It means where the variable is contained and accessible or not to other objects
9.) What are semicolons used for?
signify the end of a statement

10.) What are lines of code indented? When should a line be indented?
Its used for read ability mostly. Indent so you are 4 spaces more indented than the indentation of
 the block containing objects declaration. The compiler doesnt see white space.

11.) What does a .java file need to be complied to a .class file before running?
So it is in bytecode that the jvm can understand and run







